# Core (present on Kaggle GPU images by default)
transformers>=4.57.0     # >=4.57 required for Qwen3-VL; 4.49 ok for Qwen2.5-VL
torch
pandas
pillow

# Qwen2.5-VL vision helper
qwen-vl-utils

# Optional, improves accuracy / robustness
accelerate
bitsandbytes        # only if LOAD_IN_4BIT=True
javalang            # more accurate Java structural parsing
pytesseract         # OCR augmentation (needs tesseract-ocr system package)
# paddleocr         # alternative OCR engine
# opencv-python-headless
